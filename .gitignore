# Secrets — never commit
.env

# System noise
.DS_Store
**/__pycache__/
*.pyc

# Cached Capital.com session token (8-min TTL, regenerated on demand)
state/.capital_session.json
# Daemon logs
logs/
# Daemon control files + runtime caches (local-only)
state/forex_watcher.control
state/forex_position_sync.control
state/.watcher_runtime.json
state/.position_sync_runtime.json
# Claude session stuff
.claude/
.playwright-mcp/
state/forex_events_consumed.txt
state/forex_event_waker.control
state/forex_heartbeat.control
state/.forex_claude.lock
state/.news_runtime.json
state/forex_news.control
