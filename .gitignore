# env secrets
.env
.env.local

# python
__pycache__/
*.pyc
.venv/

# node / vite
node_modules/
dashboard/dist/
dashboard/tsconfig.tsbuildinfo
*.log
logs/

# session / runtime caches
state/.capital_session.json
state/.watcher_runtime.json
state/.news_runtime.json
state/.position_sync_runtime.json
state/.counterfactual_cursor
state/.forex_claude.lock

# screenshots / playwright outputs
*.jpeg
*.png
.playwright-mcp/

# macOS
.DS_Store

# SQLite binary — rebuilt via db/migrate.py if lost; docs/data/forex/db_snapshot.json is the git-durable backup
state/pulse.db
state/pulse.db-*
state/.db_persister_cursor/
